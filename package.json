{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "api-gateway",
  "main": "index.js",
  "scripts": {
    "start-debug": "node --inspect --debug-brk api-gateway.js",
    "start": "node api-gateway.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.15.2",
    "grpc": "^1.10.1"
  }
}
